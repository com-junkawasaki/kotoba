# Threat model

The stack-wide threat model and its machine-readable evidence are owned and
checked by [`kotoba-lang/security`](https://github.com/kotoba-lang/security).

Kotoba owns the language runtime, capability host, and host adapters. It
consumes the assurance controls; it does not duplicate their implementation or
qualification evidence.
