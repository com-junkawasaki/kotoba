(ns demo-buffer-abi {:kotoba/raw-memory :implements-buffer-abi})

(defn main []
  (let [buf (bytes-ptr [0 0 0 0])]
    (+ (clipboard-read buf 4)
       (mem-byte-at buf 0)
       (mem-byte-at buf 1)
       (mem-byte-at buf 2))))
