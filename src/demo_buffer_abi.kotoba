(ns demo-buffer-abi {:kotoba/raw-memory :checked-extents})

(defn main []
  (let [buf (alloc 4)]
    (+ (clipboard-read buf 4)
       (mem-byte-at buf 0)
       (mem-byte-at buf 1)
       (mem-byte-at buf 2))))
