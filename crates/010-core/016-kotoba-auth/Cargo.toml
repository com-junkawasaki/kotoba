[package]
name = "kotoba-auth"
version = "0.1.0"
edition = "2021"
description = "Authentication and Authorization engine for the Kotoba ecosystem (ReBAC + ABAC)."
license = "Apache-2.0"

[dependencies]
kotoba-types = { path = "../010-kotoba-types" }
kotoba-errors = { path = "../011-kotoba-errors" }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
anyhow = "1.0"
sha2 = { workspace = true }
hex = { workspace = true }

[dev-dependencies]
tokio = { workspace = true, features = ["full"] }
