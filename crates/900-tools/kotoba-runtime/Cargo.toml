[package]
name = "kotoba-runtime"
version = "0.1.0"
edition = "2021"

[dependencies]
# Kotoba ecosystem - Language and Tools layers
kotoba-kotobas = { path = "../../400-language/kotoba-kotobas", version = "0.1.17" }
kotoba-cli = { path = "../kotoba-cli", version = "0.1.22" }
# kotoba-server = { path = "../../500-services/kotoba-server", version = "0.1.21" } # TODO: Add back when deploy issues are fixed
kotoba2tsx = { path = "../../400-language/kotoba2tsx", version = "0.1.21" }

anyhow = "1.0"
tokio = { version = "1.0", features = ["full"] }
clap = { version = "4.0", features = ["derive"] }
serde_json = "1.0"
