[package]
name = "kotoba-runtime"
version = "0.1.0"
edition = "2021"

[dependencies]
kotoba-kotobas = { path = "../../200-application/kotoba-kotobas" }
kotoba-cli = { path = "../../200-application/kotoba-cli" }
# kotoba-server = { path = "../../200-application/kotoba-server" } # TODO: Add back when deploy issues are fixed
kotoba2tsx = { path = "../kotoba2tsx" }

anyhow = "1.0"
tokio = { version = "1.0", features = ["full"] }
clap = { version = "4.0", features = ["derive"] }
serde_json = "1.0"
